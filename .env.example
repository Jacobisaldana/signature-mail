# Frontend (client) — public keys only
# Supabase anon key is safe in the browser
VITE_SUPABASE_URL= # e.g., https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_AVATARS_BUCKET=avatars
VITE_SUPABASE_ICONS_BUCKET=icons

# Optional (not injected by default)
GEMINI_API_KEY=

# Supabase (server-side). Never expose the service role key to the client
SUPABASE_URL= # usually the same as VITE_SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY=
